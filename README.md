# Setup
---

## Dependencies
---
* git (version control)
* zoxide (better `cd`)
* fzf (fuzzy finder)
* eza (better `ls`)
* yazi (file manager)
* bat (better `cat`)

## Installation
---
```bash
git clone https://github.com/NomadAvian/dotfiles
cd dotfiles
sudo chmod +x ./install.sh
./install.sh
```
